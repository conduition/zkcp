[package]
name = "dlog_secp256k1_sudoku"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "1.1.2", default-features = false, features = ['std'] }
common = { path = "../common", features = ["secp256k1"] }
chacha20 = "0.9.1"
